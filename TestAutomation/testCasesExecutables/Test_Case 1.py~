import sys
from mercurial import progress

testVal = sys.argv[1]
oracleVal = sys.argv[2]

outcome = progress.fmtremaining(testVal) == oracleVal

print outcome

#sys.stdout.write(outcome)
#sys.stdout.flush()
#sys.exit(0)

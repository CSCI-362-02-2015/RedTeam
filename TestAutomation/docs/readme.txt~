Read-me for for Red Team's Mercurial project

1. Building Mercurial

To build Mercurial, follow the steps below. If you are changing the source code, you must also follow the below steps before the test script will execute the given code.

i.Install Python developer module

$ sudo apt-get install python-dev

ii. Navigate to the /src directory. From there, execute the build using the following command:

$ sudo make install

2. Running the test script

Navigate to the /scripts directory
Execute the following code:

$ python runAllTests.py
